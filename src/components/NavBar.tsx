const NavBar = () => {
  return (
    <nav className={`mx-auto grid justify-center items-center max-w-3xl h-12`}>
      <h3 className={`font-light`}>
        fair<span className={`text-amber-500 font-medium`}>dinkum</span>
      </h3>
    </nav>
  );
};

export default NavBar;
